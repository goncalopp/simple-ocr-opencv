language: python
required: sudo
python:
    - "2.7"
    - "3.4"
    - "3.5"
    - "3.6"
virtualenv:
    system_site_packages: true
install:
  - sudo apt-get install python-opencv
script: nosetests
after_success:
  - coverage run nosetests
  - coveralls

